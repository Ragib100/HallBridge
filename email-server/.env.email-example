# HallBridge Email Server Configuration
# This file is used by email_server.js (separate Node.js service)
# Not used by the main Next.js application

# SMTP Server Configuration
# For Gmail: smtp.gmail.com
# For Outlook: smtp-mail.outlook.com
# For other providers: check their documentation
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# SMTP Authentication
# For Gmail: Use App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Email Sender Information
EMAIL_FROM=HallBridge <your-email@gmail.com>

# API Security
# This secret must match EMAIL_API_SECRET in the main .env file
# Generate a strong random string (e.g., using: openssl rand -hex 32)
EMAIL_API_SECRET=super-long-random-secret-change-in-production

# Server Port (optional, defaults to 3001)
# PORT=3001